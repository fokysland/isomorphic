{
  "name": "iso",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "lerna run dev --scope @iso/web",
    "build:web": "lerna run build --scope @iso/web"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.2.0"
  }
}
